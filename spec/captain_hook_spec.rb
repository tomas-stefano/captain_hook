require "spec_helper"

describe CaptainHook do
  it "has a version number" do
    expect(CaptainHook::VERSION).not_to be nil
  end

  it "does something useful" do
    expect(false).to eq(true)
  end
end
